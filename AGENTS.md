# learning-notes

Personal learning notes covering deep learning, computer vision, generative/diffusion models, optimal transport, data structures, and paper reading summaries.

> Initialized by the `mine-init-codex-project` skill on 2026-08-18.
> Edit this file to describe your project's structure, conventions, and any
> durable team agreements Codex should follow when working in this repo.

## 项目结构

<!-- TODO: 用一两段说明仓库的目录划分与各子模块的用途 -->

## 项目规则

具体可执行的项目规则（操作约束、目录使用约定等）放在 `project rules/RULES.md`，按需查阅和扩展。

临时文件统一放在仓库根目录下的 `codex temp/`（已在 `.gitignore` 中忽略其内容）。

## 使用约定

<!-- TODO: 列几条本项目特有的约定，例如：构建命令、测试入口、提交前自检、依赖管理等 -->
